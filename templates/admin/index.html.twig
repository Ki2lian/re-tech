{% extends 'base_admin.html.twig' %}
{% set iDontKnow = infos | json_encode %}
{% block title %}Tableau de bord{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="page-title-box">
                <div class="page-title-right">
                    <ol class="breadcrumb m-0">
                        <li class="breadcrumb-item active">Tableau de bord</li>
                    </ol>
                </div>
                <h4 class="page-title">Tableau de bord</h4>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-6">
            <div class="card">
                <div class="card-body d-flex justify-content-center">
                    <div class="w-50">
                        <canvas id="users"></canvas>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-6">
            <div class="card">
                <div class="card-body d-flex justify-content-center">
                    <div class="w-50">
                        <canvas id="annonces"></canvas>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {# <script src="{{ asset('js/vendor/Chart.bundle.min.js') }}"></script> #}
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.3.2/dist/chart.min.js"></script>
    <script>
        $(document).ready(function () {
            var users = $('#users').get(0).getContext('2d');
            var chart1 = new Chart(users, {
                type: 'pie',
                data: {
                    labels: ['Utilisateurs', 'Vendeurs'],
                    datasets: [
                        {
                            data: [{{ infos.nbAllUsers }}, {{ infos.nbAllSellers }}],
                            backgroundColor: [
                                'rgba(90, 177, 235, 0.4)',
                                'rgba(161, 74, 16 , 0.4)'
                            ],
                            borderColor: [
                                'rgba(90, 177, 235, 1)',
                                'rgba(161, 74, 16 , 1)',
                            ],
                            borderWidth: 1
                        }
                    ]
                },
                options: {
                    responsive: true,
                    plugins: {
                        title: {
                            display: true,
                            text: "Utilisateurs"
                        }
                    }
                }
            });

            function graphPie(){

            }
        });
    </script>

{% endblock %}